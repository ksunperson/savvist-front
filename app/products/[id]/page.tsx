import { useRouter } from 'next/navigation';

export default function ProductDetailPage() {
  const router = useRouter();

  // id 값이 있는지 확인하고 렌더링에 사용
  return (
    <div>
      <p>hi</p>
    </div>
  );
}
